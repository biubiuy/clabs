# include <stdio.h>
# define  MAXLINE 50

int getline(char line[],int max);
int strindex(char source[],char searchfor[]);

char pattern[] = "a,A,0"


main()
{
    void  getline()
    {char x[50]; int i = 0;
    gets(xi)
    x[0] = getchar
    for(x[i] !='/n'){
        if(x[i] == 'a'){
            i = 0}
            else{
                break;
            }
    }
            printf("Numbers of char 'a' is %d,i);
            return 0;
    }

